/**
 * Central type definitions for the application
 */

// Import and re-export gallery types explicitly
import type {
  GalleryImage,
  GalleryData,
  PublicGallerySettings,
} from './gallery'

export type { GalleryImage, GalleryData, PublicGallerySettings }

// Define any additional global types here if needed
